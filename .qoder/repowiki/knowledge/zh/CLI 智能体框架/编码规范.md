- 文件名采用 kebab-case，类名采用 PascalCase，函数/变量采用 camelCase，常量采用 UPPER_SNAKE_CASE。
- 接口定义使用 I 前缀（如 IAgent），类型定义使用 PascalCase。
- 所有模块通过各层的 index.ts 统一导出，禁止直接引用内部实现文件。
- 异步操作统一使用 async/await，错误处理使用自定义 Error 类。