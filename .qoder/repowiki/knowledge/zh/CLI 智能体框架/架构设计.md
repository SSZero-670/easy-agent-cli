- 采用严格的分层架构：cli（入口/REPL）→ core（调度）→ agents/tools/context/session → ui/permissions。
- 依赖方向单向向下，下层不依赖上层，同层间避免直接依赖。
- 每个功能层通过 `src/{layer}/index.ts` 统一导出公共 API，内部实现细节对外隐藏。
- 核心入口位于 `src/cli/index.ts`，负责命令解析与交互循环。